# Input values required for command execution
inputs:
  git_repo: "/sdata/opt/command-runner/dp_pipelines"

tasks:
  my_command:
    - command: >
        echo "I am ready to work"
        && echo "line 2"
        && echo "line 3"
        && echo {run_date}
